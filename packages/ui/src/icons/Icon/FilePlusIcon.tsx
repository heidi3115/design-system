import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import FilePlus from '../svg/FilePlus.svg';

export const FilePlusIcon = (props: IconProps) => <CreateIcon Icon={FilePlus} viewBox="0 0 20 20" {...props} />;
