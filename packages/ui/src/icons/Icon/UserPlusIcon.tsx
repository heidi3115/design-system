import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import UserPlus from '../svg/UserPlus.svg';

const UserPlusIcon = (props: IconProps) => <CreateIcon Icon={UserPlus} viewBox="0 0 20 20" {...props} />;
export default UserPlusIcon;
