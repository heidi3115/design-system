import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Slash from '../svg/Slash.svg';

export const SlashIcon = (props: IconProps) => <CreateIcon Icon={Slash} viewBox="0 0 20 20" {...props} />;
