import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import PieChart from '../svg/PieChart.svg';

export const PieChartIcon = (props: IconProps) => <CreateIcon Icon={PieChart} viewBox="0 0 20 20" {...props} />;
