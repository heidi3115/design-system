import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Lock from '../svg/Lock.svg';

export const LockIcon = (props: IconProps) => <CreateIcon Icon={Lock} viewBox="0 0 20 20" {...props} />;
