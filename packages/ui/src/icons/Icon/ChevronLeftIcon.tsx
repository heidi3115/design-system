import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';
import ChevronLeft from '../svg/ChevronLeft.svg';

export const ChevronLeftIcon = (props: IconProps) => <CreateIcon Icon={ChevronLeft} viewBox="0 0 20 20" {...props} />;
