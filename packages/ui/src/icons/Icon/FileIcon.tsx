import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import File from '../svg/File.svg';

const FileIcon = (props: IconProps) => <CreateIcon Icon={FileIcon} viewBox="0 0 20 20" {...props} />;
export default FileIcon;
