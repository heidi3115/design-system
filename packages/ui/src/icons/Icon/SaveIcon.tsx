import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Save from '../svg/Save.svg';

const SaveIcon = (props: IconProps) => <CreateIcon Icon={Save} viewBox="0 0 20 20" {...props} />;
export default SaveIcon;
