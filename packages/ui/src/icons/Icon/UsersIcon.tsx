import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Users from '../svg/Users.svg';

export const UsersIcon = (props: IconProps) => <CreateIcon Icon={Users} viewBox="0 0 20 20" {...props} />;
