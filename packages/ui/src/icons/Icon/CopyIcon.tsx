import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Copy from '../svg/Copy.svg';

const CopyIcon = (props: IconProps) => <CreateIcon Icon={Copy} viewBox="0 0 20 20" {...props} />;
export default CopyIcon;
