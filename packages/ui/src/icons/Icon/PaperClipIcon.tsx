import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import PaperClip from '../svg/PaperClip.svg';

export const PaperClipIcon = (props: IconProps) => <CreateIcon Icon={PaperClip} viewBox="0 0 20 20" {...props} />;
