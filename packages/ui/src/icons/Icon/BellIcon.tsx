import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';
import Bell from '../svg/Bell.svg';

export const BellIcon = (props: IconProps) => <CreateIcon Icon={Bell} viewBox="0 0 20 20" {...props} />;
