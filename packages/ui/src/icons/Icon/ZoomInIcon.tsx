import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import ZoomIn from '../svg/ZoomIn.svg';

export const ZoomInIcon = (props: IconProps) => <CreateIcon Icon={ZoomIn} viewBox="0 0 20 20" {...props} />;
