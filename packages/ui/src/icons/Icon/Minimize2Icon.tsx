import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Minimize2 from '../svg/Minimize2.svg';

const Minimize2Icon = (props: IconProps) => <CreateIcon Icon={Minimize2} viewBox="0 0 20 20" {...props} />;
export default Minimize2Icon;
