import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';
import CheckSquare from '../svg/CheckSquare.svg';

const CheckSquareIcon = (props: IconProps) => <CreateIcon Icon={CheckSquare} viewBox="0 0 20 20" {...props} />;
export default CheckSquareIcon;
