import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import User from '../svg/User.svg';

const UserIcon = (props: IconProps) => <CreateIcon Icon={User} viewBox="0 0 20 20" {...props} />;
export default UserIcon;
