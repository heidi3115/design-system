import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Layers from '../svg/Layers.svg';

const LayersIcon = (props: IconProps) => <CreateIcon Icon={Layers} viewBox="0 0 20 20" {...props} />;
export default LayersIcon;
