import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Eye from '../svg/Eye.svg';

const EyeIcon = (props: IconProps) => <CreateIcon Icon={Eye} viewBox="0 0 20 20" {...props} />;
export default EyeIcon;
