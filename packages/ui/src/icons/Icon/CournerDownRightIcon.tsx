import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import CornerDownRight from '../svg/CornerDownRight.svg';

export const CournerDownRightIcon = (props: IconProps) => (
  <CreateIcon Icon={CornerDownRight} viewBox="0 0 20 20" {...props} />
);
