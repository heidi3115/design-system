import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Plus from '../svg/Plus.svg';

const PlusIcon = (props: IconProps) => <CreateIcon Icon={Plus} viewBox="0 0 20 20" {...props} />;
export default PlusIcon;
