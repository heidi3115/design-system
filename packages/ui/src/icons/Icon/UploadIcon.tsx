import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Upload from '../svg/Upload.svg';

export const UploadIcon = (props: IconProps) => <CreateIcon Icon={Upload} viewBox="0 0 20 20" {...props} />;
