import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Inbox from '../svg/Inbox.svg';

const InboxIcon = (props: IconProps) => <CreateIcon Icon={Inbox} viewBox="0 0 20 20" {...props} />;
export default InboxIcon;
