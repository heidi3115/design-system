import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Printer from '../svg/Printer.svg';

const PrinterIcon = (props: IconProps) => <CreateIcon Icon={Printer} viewBox="0 0 20 20" {...props} />;
export default PrinterIcon;
