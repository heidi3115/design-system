import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import MinusSquare from '../svg/MinusSquare.svg';

const MinusSquareIcon = (props: IconProps) => <CreateIcon Icon={MinusSquare} viewBox="0 0 20 20" {...props} />;
export default MinusSquareIcon;
