import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Home from '../svg/Home.svg';

const HomeIcon = (props: IconProps) => <CreateIcon Icon={Home} viewBox="0 0 20 20" {...props} />;
export default HomeIcon;
