import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import PlusCircle from '../svg/PlusCircle.svg';

const PlusCircleIcon = (props: IconProps) => <CreateIcon Icon={PlusCircle} viewBox="0 0 20 20" {...props} />;
export default PlusCircleIcon;
