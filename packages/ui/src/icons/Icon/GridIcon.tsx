import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Grid from '../svg/Grid.svg';

export const GridIcon = (props: IconProps) => <CreateIcon Icon={Grid} viewBox="0 0 20 20" {...props} />;
