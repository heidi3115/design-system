import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import XSquare from '../svg/XSquare.svg';

export const XSquareIcon = (props: IconProps) => <CreateIcon Icon={XSquare} viewBox="0 0 20 20" {...props} />;
