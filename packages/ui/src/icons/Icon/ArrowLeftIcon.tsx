import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';
import ArrowLeft from '../svg/ArrowLeft.svg';

export const ArrowLeftIcon = (props: IconProps) => <CreateIcon Icon={ArrowLeft} viewBox="0 0 20 20" {...props} />;
