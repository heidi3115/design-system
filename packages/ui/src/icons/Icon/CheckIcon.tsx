import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';
import Check from '../svg/Check.svg';

const CheckIcon = (props: IconProps) => <CreateIcon Icon={Check} viewBox="0 0 20 20" {...props} />;
export default CheckIcon;
